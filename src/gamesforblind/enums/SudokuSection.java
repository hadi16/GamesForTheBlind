package gamesforblind.enums;

/**
 * Represents the different types of sections in a Sudoku game: the row, column, or block.
 * Important for determining which section in the game should be read off by the audio player.
 */
public enum SudokuSection {
    ROW, COLUMN, BLOCK
}
